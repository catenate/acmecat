pos git mv sync update
pos git rm -r -f 
twit github
|fmt
:,$ |sed 's, *$,,;s,$,  ,;/^  $/s,  ,,'
|sort|uniq


update
du -a | grep '/nbindex$' | sed 's,^.*  ,/,' > /acmecat/nb/nbindexes
pos git status
pos git add .
pos git commit -a -m 'Credoer waits at start to eval deps at its turn, no contention on own files.'
$ git push -u origin master


apply {diff /usr/jcatena/lib/$1 /acmecat/cygwin/$1} plumbing
apply {diff /usr/jcatena/$1 /acmecat/cygwin/$1} guide
apply {diff /n/C/cygwin/home/jcatena/bin/$1 /acmecat/cygwin/$1} emu
flag x +
