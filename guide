pos git mv sync update
pos git rm -r -f 
update
twit github
|fmt
:,$ |sed 's, *$,,;s,$,  ,;/^  $/s,  ,,'
|sort|uniq

pos git status
pos git add .
pos git commit -a -m 'Add man page for map.  Add emu per drive letter until I clean it up. Simple head.  Change $root to $ta in mkdo templates.  Explicit echo in ecmd-step.do.'
$ git push -u origin master
