pos git mv sync update
pos git rm -r -f 
twit github
|fmt
:,$ |sed 's, *$,,;s,$,  ,;/^  $/s,  ,,'
|sort|uniq

update
du -a | grep '/nbindex$' | sed 's,^.*  ,/,' > /acmecat/nb/nbindexes
pos git status
pos git add .
pos git commit -a -m 'If redo finds target in progress, wait then restart.  Add twhois and unfollow.'
$ git push -u origin master
